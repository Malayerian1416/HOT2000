class AlertBox {
    constructor(type, title = null, message) {
        this.type = type;
        this.title = title;
        this.message = message;
    }

    alert() {
        document.createElement(``);
    }

}
